SELECT DO ORGAO:
- NOME DO ORGAO (ORG_LOTACAO);
- CODIGO DO ORGAO (COD_ORG_LOTACAO);
- NOME UNIDADE (UORG_LOTACAO);
- ORG. SUPERINTENDENTE(ORGSUP_LOTACAO);

SELECT DO SERVIDOR:
- NOME DO SERVIDOR (NOME);
- MATRICULA (MATRICULA);
- CARGO (DESCRICAO_CARGO);
- CLASSE DO CARGO(CLASSE_CARGO);

SELECT DO SALARIO:
- FAIXA SALARIAL;
- 
